import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    view_list_movie: {
        flexDirection: "row",
        backgroundColor: "skyblue"
    },
    view_single_movie: {
        padding: 4,
       
    }
})